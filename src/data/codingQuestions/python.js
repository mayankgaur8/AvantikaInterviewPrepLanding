const pythonQuestions = [
  { id: 'python-1', title: 'Sum of list', statement: 'Return the sum of numbers in a list.', language: 'python', solution: `def solve(arr):\n    return sum(arr)` },
  { id: 'python-2', title: 'Two-sum', statement: 'Given nums and target, return indices of two numbers that add up to target.', language: 'python', solution: `def solve(nums, target):\n    seen = {}\n    for i, v in enumerate(nums):\n        need = target - v\n        if need in seen: return [seen[need], i]\n        seen[v] = i\n    return None` },
  { id: 'python-3', title: 'Reverse string', statement: 'Return the reverse of a string.', language: 'python', solution: `def solve(s):\n    return s[::-1]` },
  { id: 'python-4', title: 'Palindrome check', statement: 'Check if a string is a palindrome (ignore non-alphanum, case-insensitive).', language: 'python', solution: `import re\ndef solve(s):\n    t = re.sub(r'[^0-9a-zA-Z]', '', s).lower()\n    return t == t[::-1]` },
  { id: 'python-5', title: 'Fibonacci (iterative)', statement: 'Return n-th Fibonacci (0-indexed) efficiently.', language: 'python', solution: `def solve(n):\n    a,b=0,1\n    for _ in range(n): a,b=b,a+b\n    return a` },
  { id: 'python-6', title: 'Factorial', statement: 'Compute factorial of n.', language: 'python', solution: `def solve(n):\n    res=1\n    for i in range(2,n+1): res *= i\n    return res` },
  { id: 'python-7', title: 'Binary search', statement: 'Implement binary search that returns index or -1.', language: 'python', solution: `def solve(arr, target):\n    l,r=0,len(arr)-1\n    while l<=r:\n        m=(l+r)//2\n        if arr[m]==target: return m\n        if arr[m]<target: l=m+1\n        else: r=m-1\n    return -1` },
  { id: 'python-8', title: 'Merge two sorted lists', statement: 'Merge two sorted lists and return sorted result.', language: 'python', solution: `def solve(a,b):\n    i=j=0;res=[]\n    while i<len(a) and j<len(b):\n        if a[i]<b[j]: res.append(a[i]); i+=1\n        else: res.append(b[j]); j+=1\n    res.extend(a[i:]); res.extend(b[j:]);\n    return res` },
  { id: 'python-9', title: 'Remove duplicates from list (in-place)', statement: 'Remove duplicates preserving order.', language: 'python', solution: `def solve(arr):\n    seen=set(); out=[]\n    for x in arr:\n        if x not in seen:\n            seen.add(x); out.append(x)\n    return out` },
  { id: 'python-10', title: 'Max subarray (Kadane)', statement: 'Return maximum subarray sum.', language: 'python', solution: `def solve(nums):\n    best = cur = nums[0]\n    for x in nums[1:]:\n        cur = max(x, cur+x)\n        best = max(best, cur)\n    return best` },
  { id: 'python-11', title: 'Valid parentheses', statement: 'Check if brackets are balanced.', language: 'python', solution: `def solve(s):\n    pairs={'(':')','[':']','{':'}'}; stack=[]\n    for c in s:\n        if c in pairs: stack.append(c)\n        else:\n            if not stack or pairs[stack.pop()]!=c: return False\n    return not stack` },
  { id: 'python-12', title: 'Merge intervals', statement: 'Merge overlapping intervals and return merged list.', language: 'python', solution: `def solve(intervals):\n    if not intervals: return []\n    intervals.sort()\n    res=[intervals[0]]\n    for s,e in intervals[1:]:\n        last=res[-1]\n        if s<=last[1]: last[1]=max(last[1], e)\n        else: res.append([s,e])\n    return res` },
  { id: 'python-13', title: 'Top K frequent', statement: 'Return k most frequent elements from list.', language: 'python', solution: `from collections import Counter\ndef solve(nums,k):\n    cnt=Counter(nums)\n    return [x for x,_ in cnt.most_common(k)]` },
  { id: 'python-14', title: 'Product of array except self', statement: 'Return array of products except self without division.', language: 'python', solution: `def solve(nums):\n    n=len(nums); res=[1]*n\n    left=1\n    for i in range(n): res[i]=left; left*=nums[i]\n    right=1\n    for i in range(n-1,-1,-1): res[i]*=right; right*=nums[i]\n    return res` },
  { id: 'python-15', title: 'Rotate array', statement: 'Rotate array to the right by k steps.', language: 'python', solution: `def solve(nums,k):\n    k%=len(nums)\n    return nums[-k:]+nums[:-k]` },
  { id: 'python-16', title: 'Linked list reverse (iterative)', statement: 'Reverse singly linked list and return new head. (use node.val/node.next)', language: 'python', solution: `def solve(head):\n    prev=None\n    cur=head\n    while cur:\n        nxt=cur.next; cur.next=prev; prev=cur; cur=nxt\n    return prev` },
  { id: 'python-17', title: 'Detect cycle in linked list', statement: 'Return True if linked list has a cycle.', language: 'python', solution: `def solve(head):\n    slow=fast=head\n    while fast and fast.next:\n        slow=slow.next; fast=fast.next.next\n        if slow==fast: return True\n    return False` },
  { id: 'python-18', title: 'Find kth largest', statement: 'Return k-th largest element in array.', language: 'python', solution: `import heapq\ndef solve(nums,k):\n    return heapq.nlargest(k, nums)[-1]` },
  { id: 'python-19', title: 'Lowest common ancestor in BST', statement: 'Given root of BST and two values, return LCA node.', language: 'python', solution: `def solve(root,a,b):\n    while root:\n        if root.val>max(a,b): root=root.left\n        elif root.val<min(a,b): root=root.right\n        else: return root\n    return None` },
  { id: 'python-20', title: 'Inorder traversal (iterative)', statement: 'Return inorder traversal of binary tree without recursion.', language: 'python', solution: `def solve(root):\n    res=[]; stack=[]; cur=root\n    while stack or cur:\n        while cur: stack.append(cur); cur=cur.left\n        cur=stack.pop(); res.append(cur.val); cur=cur.right\n    return res` },
  { id: 'python-21', title: 'Level order traversal', statement: 'Return level order (BFS) of binary tree.', language: 'python', solution: `from collections import deque\ndef solve(root):\n    if not root: return []\n    q=deque([root]); res=[]\n    while q:\n        level=[]\n        for _ in range(len(q)): n=q.popleft(); level.append(n.val);\n            if n.left: q.append(n.left)\n            if n.right: q.append(n.right)\n        res.append(level)\n    return res` },
  { id: 'python-22', title: 'Serialize / deserialize (preorder)', statement: 'Serialize and deserialize binary tree using preorder and null markers.', language: 'python', solution: `def serialize(root):\n    vals=[]\n    def p(n):\n        if not n: vals.append('#'); return\n        vals.append(str(n.val)); p(n.left); p(n.right)\n    p(root)\n    return ','.join(vals)\n\ndef deserialize(s):\n    it=iter(s.split(','))\n    def build():\n        v=next(it)\n        if v=='#': return None\n        node=Node(int(v)); node.left=build(); node.right=build(); return node\n    return build()` },
  { id: 'python-23', title: 'Depth-first search (recursive)', statement: 'Implement DFS that returns all nodes visited in pre-order.', language: 'python', solution: `def solve(root):\n    res=[]\n    def dfs(n):\n        if not n: return\n        res.append(n.val)\n        dfs(n.left); dfs(n.right)\n    dfs(root); return res` },
  { id: 'python-24', title: 'Anagram grouping', statement: 'Group list of strings into anagrams.', language: 'python', solution: `from collections import defaultdict\ndef solve(strs):\n    d=defaultdict(list)\n    for s in strs: d[''.join(sorted(s))].append(s)\n    return list(d.values())` },
  { id: 'python-25', title: 'Longest common prefix', statement: 'Find longest common prefix among strings.', language: 'python', solution: `def solve(strs):\n    if not strs: return ''\n    s=min(strs); t=max(strs)\n    for i,c in enumerate(s):\n        if c!=t[i]: return s[:i]\n    return s` },
  { id: 'python-26', title: 'Word ladder (BFS shortest path)', statement: 'Given begin,end,wordList return shortest transformation length.', language: 'python', solution: `from collections import deque\ndef solve(begin,end,words):\n    s=set(words); q=deque([(begin,1)]);\n    while q:\n        w,d=q.popleft()\n        if w==end: return d\n        for i in range(len(w)):\n            for c in 'abcdefghijklmnopqrstuvwxyz':\n                nw=w[:i]+c+w[i+1:]\n                if nw in s: s.remove(nw); q.append((nw,d+1))\n    return 0` },
  { id: 'python-27', title: 'Combination sum (backtracking)', statement: 'Return combinations summing to target from candidates (no reuse).', language: 'python', solution: `def solve(cands,target):\n    res=[]\n    cands.sort()\n    def bt(i,cur,s):\n        if s==target: res.append(cur[:]); return\n        if s>target: return\n        for j in range(i,len(cands)):\n            cur.append(cands[j]); bt(j+1,cur,s+cands[j]); cur.pop()\n    bt(0,[],0); return res` },
  { id: 'python-28', title: 'LRU cache (builtin)', statement: 'Implement LRU cache using OrderedDict or functools.lru_cache.', language: 'python', solution: `from collections import OrderedDict\nclass LRU:\n    def __init__(self,k): self.k=k; self.d=OrderedDict()\n    def get(self,x):\n        if x not in self.d: return -1\n        self.d.move_to_end(x); return self.d[x]\n    def put(self,x,v):\n        if x in self.d: self.d.move_to_end(x)\n        self.d[x]=v\n        if len(self.d)>self.k: self.d.popitem(last=False)` },
  { id: 'python-29', title: 'Trie insert & search', statement: 'Implement basic Trie with insert and search(prefix/word).', language: 'python', solution: `class TrieNode:\n    def __init__(self): self.ch={},self.end=False\nclass Trie:\n    def __init__(self): self.root=TrieNode()\n    def insert(self,w): n=self.root;\n        for c in w: n=n.ch.setdefault(c,TrieNode()); n.end=True\n    def search(self,w): n=self.root\n        for c in w: n=n.ch.get(c); if not n: return False\n        return n.end` },
  { id: 'python-30', title: 'Topological sort', statement: 'Return topological order for DAG or detect cycle.', language: 'python', solution: `from collections import defaultdict,deque\ndef solve(edges):\n    g=defaultdict(list); indeg=defaultdict(int)\n    for u,v in edges: g[u].append(v); indeg[v]+=1; indeg.setdefault(u,0)\n    q=deque([u for u in indeg if indeg[u]==0]); res=[]\n    while q: u=q.popleft(); res.append(u);\n        for v in g[u]: indeg[v]-=1;\n            if indeg[v]==0: q.append(v)\n    return res if len(res)==len(indeg) else None` },
  { id: 'python-31', title: 'Matrix spiral order', statement: 'Return elements of matrix in spiral order.', language: 'python', solution: `def solve(a):\n    res=[]\n    while a:\n        res+=a.pop(0)\n        if not a: break\n        for r in a: res.append(r.pop())\n        a.reverse(); a=[list(x) for x in zip(*a)]\n    return res` },
  { id: 'python-32', title: 'Rotate matrix (in-place)', statement: 'Rotate NxN matrix 90 degrees clockwise in-place.', language: 'python', solution: `def solve(a):\n    n=len(a)\n    for i in range(n//2):\n        for j in range(i,n-i-1):\n            a[i][j],a[n-1-j][i],a[n-1-i][n-1-j],a[j][n-1-i]=a[n-1-j][i],a[n-1-i][n-1-j],a[j][n-1-i],a[i][j]` },
  { id: 'python-33', title: 'Find median of stream (heap)', statement: 'Design data structure to get median from stream.', language: 'python', solution: `import heapq\nclass MedianFinder:\n    def __init__(self): self.lo=[],self.hi=[]\n    def add(self,num):\n        heapq.heappush(self.lo,-num);\n        if self.lo and self.hi and -self.lo[0]>self.hi[0]: v=-heapq.heappop(self.lo); heapq.heappush(self.hi,v)\n        if len(self.lo)>len(self.hi)+1: heapq.heappush(self.hi,-heapq.heappop(self.lo))\n        if len(self.hi)>len(self.lo): heapq.heappush(self.lo,-heapq.heappop(self.hi))\n    def median(self): return -self.lo[0] if len(self.lo)>len(self.hi) else (-self.lo[0]+self.hi[0])/2` },
  { id: 'python-34', title: 'Regular expression match (simple)', statement: 'Implement simple wildcard matching with ? and * (recursive/memo).', language: 'python', solution: `from functools import lru_cache\ndef solve(s,p):\n    @lru_cache(None)\n    def m(i,j):\n        if j==len(p): return i==len(s)\n        first = i<len(s) and (p[j]=='?' or p[j]==s[i])\n        if p[j]=='*':\n            return m(i,j+1) or (i<len(s) and m(i+1,j))\n        return first and m(i+1,j+1)\n    return m(0,0)` },
  { id: 'python-35', title: 'Word break (DP)', statement: 'Given s and wordDict decide if s can be segmented.', language: 'python', solution: `def solve(s,words):\n    n=len(s); dp=[False]*(n+1); dp[0]=True\n    for i in range(1,n+1):\n        for j in range(i):\n            if dp[j] and s[j:i] in words: dp[i]=True; break\n    return dp[n]` },
  { id: 'python-36', title: 'Serialize dict to JSON-safe string', statement: 'Serialize nested dict/list to JSON string (use json.dumps).', language: 'python', solution: `import json\ndef solve(obj):\n    return json.dumps(obj, separators=(',',':'))` },
  { id: 'python-37', title: 'Concurrent worker pool (threads)', statement: 'Run N tasks with a fixed-size thread pool and collect results.', language: 'python', solution: `from concurrent.futures import ThreadPoolExecutor\ndef solve(tasks, workers=4):\n    with ThreadPoolExecutor(workers) as ex:\n        return [f.result() for f in ex.map(lambda t: t(), tasks)]` },
  { id: 'python-38', title: 'Rate limiter (token bucket sketch)', statement: 'Sketch a simple token bucket rate limiter using timestamps.', language: 'python', solution: `import time\nclass TokenBucket:\n    def __init__(self, rate, capacity):\n        self.rate=rate; self.cap=capacity; self.tokens=capacity; self.ts=time.time()\n    def allow(self):\n        now=time.time(); self.tokens=min(self.cap, self.tokens+(now-self.ts)*self.rate); self.ts=now\n        if self.tokens>=1: self.tokens-=1; return True\n        return False` },
  { id: 'python-39', title: 'Binary tree maximum depth', statement: 'Return maximum depth of binary tree.', language: 'python', solution: `def solve(root):\n    if not root: return 0\n    return 1+max(solve(root.left), solve(root.right))` },
  { id: 'python-40', title: 'Flatten nested list iterator', statement: 'Given nested lists, implement iterator that yields flattened values lazily.', language: 'python', solution: `class NestedIter:\n    def __init__(self,nested): self.stack=[iter(nested)]\n    def __iter__(self): return self\n    def __next__(self):\n        while self.stack:\n            try: x=next(self.stack[-1])\n            except StopIteration: self.stack.pop(); continue\n            if isinstance(x,list): self.stack.append(iter(x)); continue\n            return x\n        raise StopIteration` },
  { id: 'python-41', title: 'Find duplicates in array', statement: 'Return list of duplicated elements (appearing >1).', language: 'python', solution: `from collections import Counter\ndef solve(a):\n    return [x for x,c in Counter(a).items() if c>1]` },
  { id: 'python-42', title: 'Minimum window substring (sketch)', statement: 'Find minimum window in s containing all chars of t.', language: 'python', solution: `from collections import Counter\ndef solve(s,t):\n    need=Counter(t); missing=len(t); i=0; start=0; minlen=1e9\n    for j,c in enumerate(s):\n        if need[c]>0: missing-=1\n        need[c]-=1\n        while missing==0:\n            if j-i+1<minlen: start=i; minlen=j-i+1\n            need[s[i]]+=1\n            if need[s[i]]>0: missing+=1\n            i+=1\n    return s[start:start+minlen] if minlen<1e9 else ''` },
  { id: 'python-43', title: 'Sliding window max', statement: 'Return max for each sliding window of size k.', language: 'python', solution: `from collections import deque\ndef solve(nums,k):\n    res=[]; dq=deque()\n    for i,n in enumerate(nums):\n        while dq and nums[dq[-1]]<n: dq.pop()\n        dq.append(i)\n        if dq[0]<=i-k: dq.popleft()\n        if i>=k-1: res.append(nums[dq[0]])\n    return res` },
  { id: 'python-44', title: 'Count islands', statement: 'Count number of islands in binary grid.', language: 'python', solution: `def solve(grid):\n    if not grid: return 0\n    m,n=len(grid),len(grid[0]); cnt=0\n    def dfs(i,j):\n        if i<0 or j<0 or i>=m or j>=n or grid[i][j]==0: return\n        grid[i][j]=0;\n        for di,dj in [(1,0),(-1,0),(0,1),(0,-1)]: dfs(i+di,j+dj)\n    for i in range(m):\n        for j in range(n):\n            if grid[i][j]==1: cnt+=1; dfs(i,j)\n    return cnt` },
  { id: 'python-45', title: 'Permutation generation', statement: 'Return all permutations of a list.', language: 'python', solution: `def solve(nums):\n    res=[]\n    def bt(cur,rem):\n        if not rem: res.append(cur[:]); return\n        for i in range(len(rem)): bt(cur+[rem[i]], rem[:i]+rem[i+1:])\n    bt([], nums); return res` },
  { id: 'python-46', title: 'Subsets (power set)', statement: 'Return all subsets of a list.', language: 'python', solution: `def solve(nums):\n    res=[[]]\n    for x in nums: res += [r+[x] for r in res]\n    return res` },
  { id: 'python-47', title: 'Evaluate reverse polish notation', statement: 'Evaluate RPN expression tokens.', language: 'python', solution: `def solve(tokens):\n    st=[]\n    ops={'+':lambda a,b:a+b,'-':lambda a,b:a-b,'*':lambda a,b:a*b,'/':lambda a,b:int(a/b)}\n    for t in tokens:\n        if t in ops: b=st.pop(); a=st.pop(); st.append(ops[t](a,b))\n        else: st.append(int(t))\n    return st[0]` },
  { id: 'python-48', title: 'Backspace string compare', statement: 'Compare two strings after processing backspaces (#).', language: 'python', solution: `def process(s):\n    st=[]\n    for c in s:\n        if c=='#':\n            if st: st.pop()\n        else: st.append(c)\n    return ''.join(st)\ndef solve(s,t): return process(s)==process(t)` },
  { id: 'python-49', title: 'Minimum path sum in grid', statement: 'Find minimum sum path from top-left to bottom-right.', language: 'python', solution: `def solve(grid):\n    m,n=len(grid),len(grid[0])\n    dp=[[0]*n for _ in range(m)]\n    for i in range(m):\n        for j in range(n):\n            if i==0 and j==0: dp[i][j]=grid[i][j]\n            else: dp[i][j]=grid[i][j]+min(dp[i-1][j] if i else 1e9, dp[i][j-1] if j else 1e9)\n    return dp[-1][-1]` },
  { id: 'python-50', title: 'Dynamic programming - longest increasing subsequence (n log n)', statement: 'Return length of LIS in O(n log n).', language: 'python', solution: `import bisect\ndef solve(nums):\n    d=[]\n    for x in nums:\n        i=bisect.bisect_left(d,x)\n        if i==len(d): d.append(x)\n        else: d[i]=x\n    return len(d)` }
];

export default pythonQuestions;
